using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ex2 : MonoBehaviour
{
     void Start()
    {
        int i = 1;
        while (i <= 20) //1 is added in a loop until it reaches 20 and stops
        {
            Debug.Log("hola, holita, vecinito");
            i++; //1 is added
        }
    }    
}
